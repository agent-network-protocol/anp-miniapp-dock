<view class="card">
  <text class="title">{{ title }}</text>
  <text>{{ drinkName }}</text>
  <text>{{ payable }}</text>
  <button class="primary" bindtap="payOrder" data-order-id="{{ orderId }}">确认支付</button>
</view>
